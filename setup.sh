#!/bin/bash

# This script is used to set up the project environment.

# Run the depsetup.sh script with sudo privileges to install dependencies.
sudo ./depsetup.sh

# Run the envsetup.sh script to configure the project environment.
./envsetup.sh
